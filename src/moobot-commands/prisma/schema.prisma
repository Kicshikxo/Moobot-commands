generator client {
    provider = "prisma-client-js"
    output   = "./client"
}

datasource db {
    provider = "postgresql"
    url      = env("MOOBOT_DATABASE_URL")
}

model Test {
    id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid

    @@map("test")
}
